import React from 'react';
import Map from 'ol/Map';

type ActionComponentProps = {
  map: Map;
};

export const ActionComponent = ({ map }: ActionComponentProps) => {
  return <div>ooioioi</div>;
};
